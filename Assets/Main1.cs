using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Game
{
    public class Main1 : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            Debug.Log("soy la clase 2");
        }

        // Update is called once per frame
        void Update()
        {
            Debug.Log("soy la clase 2");
        }
    }
}
